# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code (local only — shared launch.json goes in .vscode/ if needed)
.vscode/settings.json
.vscode/launch.json
.vscode/tasks.json

# Flutter / Dart / Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Code generation artifacts — never commit these
**/*.g.dart
**/*.freezed.dart

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android build artifacts
/android/app/debug
/android/app/profile
/android/app/release

# Android signing — keep out of version control
android/key.properties
android/app/*.jks
android/app/*.keystore
android/local.properties

# iOS signing — managed by Xcode / CI
ios/Flutter/flutter_export_environment.sh

# Environment / secrets
.env
.env.*

# Flutter test tool
test/.dart_tool/
