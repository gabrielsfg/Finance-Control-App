/// A mutable entry used when building the links list in the wishlist form.
class WishlistLinkFormEntry {
  WishlistLinkFormEntry({this.url = '', this.storeName});

  String url;
  String? storeName;
}
